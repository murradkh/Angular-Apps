//what alaa told me thats i cant access to database in my javascript application (or angular) is wrong, indeed i can access it , like database in firebase.
//the differnce between php and angular thats in php is server side,and angular is client side,in client we send the requests,e.g by using angular,in server we reciveing the requests,e.g,by using php.
import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
}
